spring:
  profiles:
    active: cloud
---
spring:
  profiles: cloud
  application:
    name: movie
  cloud:
     uri: http://${cloud_host}:8888/
     enabled: false
encrypt:
  failOnError: false
---
spring:
  profiles: docker
  application:
    name: movie
  cloud:
    config:
      uri: http://configserver:8888
      enabled: false
encrypt:
  failOnError: false